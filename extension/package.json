{
  "name": "@shieldmail/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ShieldMail Safari Web Extension — Shield Mode signup automation",
  "scripts": {
    "dev": "NODE_ENV=development vite build --watch --mode development",
    "build": "node scripts/copy-public.cjs && NODE_ENV=production vite build -c vite.background.config.ts && NODE_ENV=production vite build -c vite.content.config.ts && NODE_ENV=production vite build -c vite.popup.config.ts",
    "build:dev": "node scripts/copy-public.cjs dev && NODE_ENV=development vite build -c vite.background.config.ts && NODE_ENV=development vite build -c vite.content.config.ts && NODE_ENV=development vite build -c vite.popup.config.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "0.1.39",
    "@types/webextension-polyfill": "0.10.7",
    "@vitest/web-worker": "1.6.1",
    "happy-dom": "14.12.3",
    "preact": "10.29.1",
    "typescript": "5.9.3",
    "vite": "5.4.21",
    "vitest": "1.6.1",
    "wrangler": "4.82.2"
  },
  "dependencies": {
    "webextension-polyfill": "0.12.0"
  }
}