{
  "name": "@shieldmail/email-router",
  "version": "0.1.0",
  "private": true,
  "description": "ShieldMail Cloudflare Worker: Hono API + Email Routing handler + AliasChannel DO. Implements M1 of the architecture.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run --project unit",
    "test:integration": "vitest run --project integration"
  },
  "dependencies": {
    "hono": "4.12.12",
    "postal-mime": "2.7.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.5.5",
    "@cloudflare/workers-types": "4.20260408.1",
    "@vitest/coverage-v8": "2.1.9",
    "typescript": "5.9.3",
    "vitest": "2.1.9",
    "wrangler": "3.114.17"
  },
  "engines": {
    "node": ">=20"
  }
}
