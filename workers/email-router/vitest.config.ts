// Top-level config kept minimal — actual project definitions live in
// vitest.workspace.ts (vitest 2.1+ workspace API).
import { defineConfig } from "vitest/config";

export default defineConfig({});
